#include <stdio.h>

int
main(
     int argc,
     char* argv[]
     )
{
  char* arg1 = argv[1];
  char* arg1n = argv[1]+2;
  char* arg2 = argv[2];
  char* arg2n = argv[2]+2;  
  
  
  fprintf(stdout,"%s", arg1n);
  printf("\n");
  
  if ( arg1[1] == 'a' && arg2[1] == 'b'){
    int arg1num = atoi(*arg1n);
    fprintf(stdout, "%i", arg1n);
  }else if ( arg1[1] == 'b' && arg2[1] == 'a'){
    
  }
  
  

  
  
  return 0;
}
